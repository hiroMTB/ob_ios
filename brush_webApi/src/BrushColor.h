#pragma once

#include "ofMain.h"

namespace ob{

namespace color{
    
    static const ofColor hour  = ofColor( 45, 95,115, 200);
    static const ofColor day   = ofColor( 83,142,116, 200);
    static const ofColor week  = ofColor(242,209,179, 200);
    static const ofColor month = ofColor( 45, 95,115, 200);
    static const ofColor year  = ofColor(255, 15, 15, 200);
}
}
